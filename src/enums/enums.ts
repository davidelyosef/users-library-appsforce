export enum Gender {
  male = 'male',
  female = 'female',
  men = 'men',
  women = 'women'
}

export enum ModalType {
  update = 'update',
  remove = 'remove',
}

export enum FilterTypes {
  email = "email",
  name = "name",
  id = "id",
  location = 'location',
}